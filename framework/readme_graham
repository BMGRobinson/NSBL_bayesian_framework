The relevant modules can be loaded and the code can be compiled by running the following script in the directory `/path-to-framework/framework/`  
` . run_graham.sh`

Running the following scripts in the directory path-to-framwork/framework/Examples/ will submit a job (job runs for ~1 minute)  
` . run_case.sh`  

If it runs successfully the output in the terminal should look something like this:  
  
```=======================================================================  
===================== Reading configuration file ======================  
  
Configuration file used : case00.cfg  
Number of proposed models: 1  
It seems that data points are stored in rows. Transposing the matrix  
It seems that data points are stored in rows. Transposing the matrix  
P used: 0.00434971  
   0.0129  
   0.0064  
Rejection ratio 0.64196  
...  
P used: 1  
   1.9102e-08  
   3.5916e-08  
Rejection ratio 0.13012 ```
